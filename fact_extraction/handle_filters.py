# coding=utf-8

import logging
import re

from string import punctuation


logger = logging.getLogger(__name__)


def normalize_all_params(raw_parameters):
    facts = raw_parameters

    return facts
